<h1>ユーザー詳細ページ</h1>
<h3><%= @user.name %></h3>
<h2>投稿内容</h2>
<hr>
<% @user.posts.each do |post| %>
  <a href="/posts/<%= post.id %>"><%= post.content %></a>
  <hr>
<% end %>
<%= link_to "ホームへ戻る", posts_path %>